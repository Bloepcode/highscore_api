name: Pull Request auto-test

on:
  pull_request:
    branches: [main]

jobs:
  test_pull_request:
    runs-on: ubuntu
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
      with:
        node-version: '14'
      - run: npm ci
      - run: npm test
